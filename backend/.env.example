# Backend Server Configuration
PORT=3001

# MySQL Database Configuration
DB_HOST=db
DB_PORT=3306
DB_NAME=game_db
DB_USER=game_user
DB_PASS=changeme_secure_password

# MySQL container initialization (used by the docker-compose db service)
# These must be present so the official MySQL image can initialize the database.
MYSQL_ROOT_PASSWORD=changeme_secure_password
MYSQL_DATABASE=game_db
MYSQL_USER=game_user
MYSQL_PASSWORD=changeme_secure_password

# CORS Configuration (frontend URL)
CORS_ORIGIN=http://localhost:5173
